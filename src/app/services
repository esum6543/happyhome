export const metadata = { title: "Services • HappyHomeByEnela" };

export default function Services() {
  return (
    <main className="mx-auto max-w-4xl px-6 py-12">
      <h1 className="text-3xl font-bold">Services</h1>
      <p className="text-slate-600 mt-2">
        Simple packages that make your space calm, functional, and beautiful.
      </p>

      <section id="refresh" className="mt-8 grid gap-4 md:grid-cols-2">
        <div className="rounded-xl border p-5">
          <h2 className="text-xl font-semibold">Room Refresh</h2>
          <p className="text-slate-600 mt-2">
            Fast makeover: layout, color palette, styling, and décor. We shop your home
            first and add only what’s needed.
          </p>
          <ul className="mt-3 list-disc ml-5 text-sm text-slate-700 space-y-1">
            <li>Furniture layout & styling</li>
            <li>Color & textiles guidance</li>
            <li>Décor sourcing (optional)</li>
          </ul>
        </div>

        <div id="organize" className="rounded-xl border p-5">
          <h2 className="text-xl font-semibold">Home Organization</h2>
          <p className="text-slate-600 mt-2">
            Closets, pantry, garage, and whole-home systems—labeled and easy to maintain.
          </p>
          <ul className="mt-3 list-disc ml-5 text-sm text-slate-700 space-y-1">
            <li>Declutter & zone planning</li>
            <li>Containers & labels (affiliate savings)</li>
            <li>Family-friendly systems</li>
          </ul>
        </div>

        <div id="photos" className="rounded-xl border p-5 md:col-span-2">
          <h2 className="text-xl font-semibold">Photo Digitizing</h2>
          <p className="text-slate-600 mt-2">
            High-quality scanning with dust cleanup and precise auto-crop. Delivered as
            shareable digital albums.
          </p>
          <ul className="mt-3 list-disc ml-5 text-sm text-slate-700 space-y-1">
            <li>600 DPI recommended (archival)</li>
            <li>Color correction & rotation</li>
            <li>Private download link or USB</li>
          </ul>
        </div>
      </section>

      <div className="mt-10 text-center">
        <a href="/contact" className="inline-block rounded-full bg-pink-500 px-5 py-3 text-white font-semibold">
          Get a free quote
        </a>
      </div>
    </main>
  );
}
