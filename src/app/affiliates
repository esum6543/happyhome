export const metadata = { title: "Affiliates • HappyHomeByEnela" };

const affiliates = [
  { name: "Amazon", url: "https://www.amazon.com/?tag=YOURTAG-20", note: "Favorite organizers & supplies" },
  { name: "The Container Store", url: "https://www.containerstore.com/?aid=YOURID", note: "Bins, labels, closet systems" },
  { name: "Local Partner", url: "https://example.com/?ref=yourcode", note: "Preferred local vendor" },
];

export default function Affiliates() {
  return (
    <main className="mx-auto max-w-4xl px-6 py-12">
      <h1 className="text-3xl font-bold">Affiliate Partners</h1>
      <p className="text-slate-600 mt-2">
        These are products and stores we genuinely use. Some links are affiliate links, which
        may earn us a small commission at no extra cost to you.
      </p>

      <div className="mt-6 grid gap-4 sm:grid-cols-2">
        {affiliates.map((a) => (
          <a key={a.name} href={a.url} className="rounded-xl border p-5 hover:bg-slate-50 transition">
            <div className="text-lg font-semibold text-pink-600">{a.name}</div>
            <div className="text-sm text-slate-600 mt-1">{a.note}</div>
          </a>
        ))}
      </div>
    </main>
  );
}
